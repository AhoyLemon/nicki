if testing == undefined
  - const testing = true
- const siteTitle = "Is this Nicki Minaj?"
- const siteURL = "https://ahoylemon.github.io/nicki"
- const description = "88.6% of Nicki Minaj fans get this wrong! Will you?"
- const d = new Date()
- const lastUpdated = d.getFullYear() + '-' + (d.getMonth()<10?'0':'') + d.getMonth() + '-' + (d.getDate()<10?'0':'') + d.getDate() + '@' + (d.getHours()<10?'0':'') + d.getHours() + ':' + (d.getMinutes()<10?'0':'') + d.getMinutes() + ':' + (d.getSeconds()<10?'0':'') + d.getSeconds()